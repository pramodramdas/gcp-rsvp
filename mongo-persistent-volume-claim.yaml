apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-persistent-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # selector:
  #   matchLabels:
  #     type: local